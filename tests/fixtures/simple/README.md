# Simple Fixture Workspace

This directory is the manual smoke-test workspace for Local Knowledge IDE v0.1.

It contains exactly two Markdown files and one plain-text file:
- `README.md` (this file)
- `Notes/hello.md`
- `ignored.txt` (plain text, must not appear in the file tree)
